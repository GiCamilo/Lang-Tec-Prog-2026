include <stdio.h>
#include <stdlib.h>
#include <math.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {

		int a, b, c;
        int resultado;
	
	printf ("Insira os valores de A, B, C:   ");
	scanf("%d %d %d", &a, &b, &c );
	
	if (a > b){
		resultado = a;
	}
	else{
		resultado = b;
	}	
	if (c > resultado){
		resultado = c;
	}

	
	
	printf ("%d eh o maior", resultado);
	
	//exercicio 2
	
	int numero;
	int r;
	
	printf("\n""Insira um numero:  ");
	scanf ("\n""%d", &numero);
	
	if (numero > 0 ){
	r = numero*-1;
     }
     else {
     	r = numero*numero;
	 }
	 
	 printf ("%d", r);


	
	
	
	return 0;
}


//condicionais parte 2
#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	/*
	int numero;
	
	printf ("Insira um numero: "),
	scanf("%d", &numero);
	
	//equivalente (n>= 1 && n<=9)
	
	if (numero < 10 && numero > 1)
		if (numero==1||numero==2 || numero==3 || numero==5 || numero==7)
			printf("O dobro de %d = %d", numero, (numero*2));
		else if(numero%2== 0 )
		   printf("%d+2 =%d", numero, numero+2);
		
		else
			printf("|%d|%d|%d|", numero-1, numero, numero+1);
		
	else printf("_out_of_range");
	
	*/
	
	
	
	char letra;
	scanf("%c", &letra);
	if (letra == 'a')printf("A de Amor");
	else if (letra == 'b')printf("B de baixinho");
	else if (letra'c')printf("C de cora��o");
	
	
	
	
	
	
	
	
	char op;
	scanf("%c", &op);
	switch (op){
	case 'a': printf("opa ta falando com ele"); break;
	
	case 'b': printf("Benedito"); break;
	
	case 'c': printf("E complicada a situa��o"); break;
	
	case 'd': printf("tamo junto"); break;
}
	

	
	
	
	
	
	
	
	
	
	return 0;
}

