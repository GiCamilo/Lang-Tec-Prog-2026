#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	//exercicio 1
	
	int primeiro, segundo, aux;
	
	printf ("Digite o primeiro valor:  ");
	scanf ("%d", &primeiro);
	printf ("Digite o segundo valor:  ");
	scanf ("%d", &segundo);
	
	aux = primeiro;
	primeiro = segundo;
	segundo = aux;
	
	printf ("%d \n %d", primeiro, segundo);
	
	
	//exercicio 2
	float primeiroV, resultado;
	double numeroD;
	
	
	printf ("\n" "Digite o primeiro valor:  ");
	scanf ("%f", &primeiroV);

	
	numeroD = primeiroV + primeiroV;
	resultado= numeroD * pow(10, 4);
	
	printf ("%lf", resultado);
	
	//exercicio 3
	int n;
	int binario[100];
	int i = 0;
	
	printf ("\n""Digite um numero inteiro:  ");
	scanf ("%d", &n);
	
	if (n == 0) {
		printf ("\n""O numero binario e:  0");
} else {
	while (n > 0){
		binario[i] = n % 2;
		n = n/2;
		i++;
	} 
	printf(" O numero binario e:");
	for (i = i - 1; i >= 0; i--){
		printf("%d", binario[i]);
     	}
     }
     
    
     
 
	
	

	
	//exercicio 4
	
 
	
	
	
	return 0;
}
